// Se houver somente um parâmetro, podemos remover os parênteses do argumento e o return
// Se a expressão for pequena, pode até ser feita em uma linha sem prejudicar a leitura do código

const raizQuadrada = x => x * x;
console.log(raizQuadrada(5));
console.log(raizQuadrada(7));